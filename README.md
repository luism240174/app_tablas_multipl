# Notas:

```
Options:
      --help        Show help                                          [boolean]
      --version     Show version number                                [boolean]
  -f, --factor_1    Es el factor constante de la tabla de multiplicar
                                                             [number] [required]
  -h, --limFactor2  Opcion para limitar la tabla. No incluye el numero
                    especificado                          [number] [default: 11]
  -l, --listar      Opcion para listar la tabla en consola
                                                      [boolean] [default: false]
```